<tr id="<%= dom_id(food) %>">
  <td><%= food.name %></td>
  <td><%= food.measurement_unit %></td>
  <td><%= "$ #{food.price}" %></td>
  <td>
    <%= link_to "Delete", food, method: :delete, class: "delete-link", data: { confirm: "Are you sure?" } %>
  </td>
</tr>
