<h1>New Inventory</h1>

<%= form_with(model: @inventory, url: inventories_path, method: :post) do |form| %>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <%= form.submit 'Create Inventory' %>
<% end %>

